extends layouts/main

block content
 .form-container_change_password
        form(action=`/users${routes.change_password}`, method="post")
            input(type="password", name="oldPasswod", placeholder="Current Password")
            input(type="password", name="newPassword", placeholder="New Password")
            input(type="password", name="newPassword1", placeholder="Verify New Password")
            input(type="submit", value="Change Password").change_password_buttom